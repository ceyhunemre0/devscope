You are an expert software engineer writing a commit message for the changes below.

Follow these rules:
- Use Conventional Commits format: `<type>(<scope>): <subject>`.
- Allowed types: feat, fix, chore, docs, refactor, test, perf, style, build, ci.
- Subject line: lowercase, no trailing period, imperative mood, max 72 chars.
- After the subject, leave a blank line and add a short body (2-4 sentences) only if the change needs explanation. Body lines wrap at 80 chars.
- Focus on WHY the change is being made, not a line-by-line WHAT.
- Output ONLY the commit message. No preamble, no code fences, no explanations.

Repository status (`git status --short`):
{% if status %}
```
{{ status }}
```
{% else %}
_(no status output)_
{% endif %}

Diff:
{% if diff %}
```diff
{{ diff }}
```
{% else %}
_(no tracked diff; likely only untracked files)_
{% endif %}
{% if truncated %}

NOTE: The diff above was truncated. Base your message on the visible portion plus the status output.
{% endif %}
